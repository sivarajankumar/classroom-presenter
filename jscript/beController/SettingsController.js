/* The settings controller manages all of the saved settings
 * for each of the users. This will then interact with a table
 * in the database the has the settings saved so that they stay
 * constant across all sessions. 
 */
